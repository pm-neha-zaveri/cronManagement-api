<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cronmanagement.dao.CronDetailsDAO">
	<select id="getCronDetails" resultType="cronmanagement.bean.CronJob">
		SELECT id as cronId,
		command as cronCommand,
		serverId as serverId,
		comment as cronComment,
		status as cronStatus,
		name as cronName,
		expression as cronExpression
		FROM cron_details;
	</select>

	<select id="getCronDetailsByServerId" resultType="cronmanagement.bean.CronJob">
		SELECT id as
		cronId,
		command as cronCommand,
		serverId as serverId,
		comment as
		cronComment,
		status as cronStatus,
		name as cronName,
		expression as
		cronExpression
		FROM cron_details where serverId = #serverId;
	</select>

	<select id="getCronDetailsByCronId" resultType="cronmanagement.bean.CronJob">
		SELECT id as
		cronId,
		command as cronCommand,
		serverId as serverId,
		comment as
		cronComment,
		status as cronStatus,
		name as cronName,
		expression as
		cronExpression
		FROM cron_details where serverId = #cronId;
	</select>
</mapper>