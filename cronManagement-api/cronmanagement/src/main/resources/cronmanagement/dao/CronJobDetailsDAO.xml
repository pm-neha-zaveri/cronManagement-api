<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cronmanagement.dao.CronJobDetailsDAO">
	<select id="getCronJobDetails" resultType="cronmanagement.bean.CronJob">
		SELECT id as cronId,
		serverId as serverId,
		comment as cronComment,
		status as cronStatus,
		name as cronName,
		scheduleexpr as cronExpression,
		cronType as cronType
		FROM cronjob;
	</select>

	<select id="getCronJobDetailsByServerId" resultType="cronmanagement.bean.CronJob">
		SELECT id as cronId,
		serverId as serverId,
		comment as cronComment,
		status as cronStatus,
		name as cronName,
		scheduleexpr as cronExpression,
		cronType as cronType
		FROM cronjob where serverId = #{serverId}
	</select>

	<select id="getCronJobDetailsByCronId" resultType="cronmanagement.bean.CronJob">
		SELECT id as cronId,
		serverId as serverId,
		comment as cronComment,
		status as cronStatus,
		name as cronName,
		scheduleexpr as cronExpression,
		cronType as cronType
		FROM cronjob where id = #{cronId};
	</select>
	
	<insert id="saveAllCronJobs">
		INSERT INTO cronjob(serverId,comment,status,name,scheduleexpr,cronType)
		VALUES (
		<foreach item="cronJob" index="index" collection="cronJobs"
			open="" close="" separator="),(">
			#{cronJob.serverId}, #{cronJob.cronComment}, #{cronJob.cronStatus},#{cronJob.cronName}, #{cronJob.cronExpression}, #{cronJob.cronType}
		</foreach>
		)
	</insert>
	
	<update id="updateAllCronJobs">
		UPDATE cronjob
		set status='INACTIVE' where id in  (
		<foreach item="cronJob" index="index" collection="cronJobs"
			open="" close="" separator=",">
			#{cronJob.cronId}
		</foreach>
		)
	</update>
	
	<select id="getCronJobDetailsByCronType" resultType="cronmanagement.bean.CronJob">
		SELECT id as cronId,
		serverId as serverId,
		comment as cronComment,
		status as cronStatus,
		name as cronName,
		scheduleexpr as cronExpression,
		cronType as cronType
		FROM cronjob where cronType like #{cronType}
	</select>

</mapper>