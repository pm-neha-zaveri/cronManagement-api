<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cronmanagement.dao.CronLogHistoryDAO">
	<select id="getCronLogHistory" resultType="cronmanagement.bean.CronLogBean">
		SELECT id as id,
		cron_id as cronId,
		server_id as serverId,
		start_time as startTime,
		end_time as endTime,
		run_time as runTime,
		threshold as threshold,
		processId as processId
		FROM cron_log_history
	</select>
	
	<select id="getCronLogHistoryByCronId" resultType="cronmanagement.bean.CronLogBean">
		SELECT id as id,
		cron_id as cronId,
		server_id as serverId,
		start_time as startTime,
		end_time as endTime,
		run_time as runTime,
		threshold as threshold,
		processId as processId
		FROM cron_log_history where cron_id=#{cronId}
	</select>
	
	<select id="getCronLogHistoryByServerId" resultType="cronmanagement.bean.CronLogBean">
		SELECT id as id,
		cron_id as cronId,
		server_id as serverId,
		start_time as startTime,
		end_time as endTime,
		run_time as runTime,
		threshold as threshold,
		processId as processId
		FROM cron_log_history where server_id=#{serverId}
	</select>
	
	
	<insert id="insertCronLogHistory">
		INSERT INTO
		cron_run_history (
		cron_id,server_id,start_time,end_time,run_time,threshold,processId
		)
		VALUES (
		<foreach item="cronLog" index="index" collection="cronLogs"
			open="" close="" separator="),(">
			#{cronLog.cronId},#{cronLog.serverId}, #{cronLog.startTime}, #{cronLog.endTime}, #{cronLog.runTime},#{cronLog.threshold}, #{cronLog.processId}
		</foreach>
		)
	</insert>
	
</mapper>