<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pubmatic.cronmanagement.dao.cronmanagementDao">

	<!-- <select id="getLineItemDetails" parameterType="map"
		resultType="com.pubmatic.premiumproposal.bean.LineItemDetails">
		
		<![CDATA[
		SELECT pub_id pubId, line_item_id lineItemId, entity_id entityId, entity_type entityType 
		FROM line_item_entity lie, line_item li 
		WHERE li.id = lie.line_item_id 
		AND li.deleted = 0 
		AND entity_type in ('SITE', 'ADTG')  
		AND line_item_id = #{lineItemId} 
		AND li.pub_id = #{pubId}
		]]>
	</select>
	
	<update id="increaseLineItemUsedCountByOne" parameterType="java.lang.Long">
		UPDATE line_item
        SET used_count = used_count+1
        WHERE id = #{lineItemId}
	</update> -->

</mapper>
